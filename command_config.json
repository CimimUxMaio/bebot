{
    "Interpreted": {
        "description": "The command can be interpreted by a given sentence. Command parameters must be given between '\"'",
        "commands": [
            {
                "name": "play",
                "parameters": ["song_name"],
                "description": "Plays the given music / video"
            },

            {
                "name": "skip",
                "parameters": [],
                "description": "Skips current song"
            },

            {
                "name": "queue",
                "parameters": [],
                "description": "Displays the current music queue"
            }
        ]
    },
    
    "Manual": {
        "description": "The exact command name or alias should be passed for the command to work",
        "commands": [
            {
                "name": "teach_last",
                "parameters": ["classification"],
                "description": "Classifies the last uninterpreted command"
            }
        ]
    }
}