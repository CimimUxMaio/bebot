{
    "paramWrappers": ["\"", "\""],

    "categories": {
        "interpreted": "The command can be interpreted by a given sentence. Command parameters must be given between \" \"",
        "manual": "The exact command name or alias should be passed for the command to work"
    },

    "commands": {
      "play": {
        "parameters": ["*song_name"],
        "optionals": [],
        "description": "Plays the given music / video"
      },

      "skip": {
        "parameters": [],
        "optionals": [["song_index", 1]],
        "description": "Skips current song"
      },

      "queue": {
        "parameters": [],
        "optionals": [],
        "description": "Displays the current music queue"
      },

      "teach_last": {
        "parameters": ["classification"],
        "optionals": [],
        "description": "Classifies the last uninterpreted command"
      },

      "help": {
        "parameters": [],
        "optionals": [],
        "description": "Shows this message"
      }
    }
}