{
    "time": {
        "type": "INTERPRETED",
        "parameters": [],
        "description": "Returns the current server time and date"
    },

    "ping": {
        "type": "INTERPRETED",
        "parameters": [],
        "description": "Returns the senders latency against the discord server"
    },

    "play": {
        "type": "INTERPRETED",
        "parameters": ["song_name"],
        "description": "Plays the given music / video"
    },

    "classify_last": {
        "type": "MANUAL",
        "description": "Classifies the last uninterpreted command; valid classifications are the names of the INTERPRETED commands"
    },

    "skip": {
        "type": "MANUAL",
        "description": "Skips current song"
    }
}